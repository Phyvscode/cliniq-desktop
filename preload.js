// Preload script - runs before the web page loads
// Keeps Node.js isolated from the frontend for security
window.addEventListener("DOMContentLoaded", () => {
  // You can expose safe APIs to the frontend here if needed
});